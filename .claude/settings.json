{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsc --noEmit --pretty false 2>&1 | head -20 || true"
          }
        ],
        "description": "Type-check after file edits to catch errors immediately"
      }
    ]
  }
}
